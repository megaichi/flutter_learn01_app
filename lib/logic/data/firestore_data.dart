class FireStoreData {
  /// ID
  String id;

  FireStoreData({this.id});

  FireStoreData.create();

// static DateTime parseTime(dynamic date) {
//   return Platform.isIOS ? (date as Timestamp).toDate() : (date as DateTime);
// }
}
